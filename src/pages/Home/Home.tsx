import React from 'react';
import { Button } from '@material-ui/core';

import './home.scss';

const Home = () => {
	return (
		<div className="home">
			Home Page
			<Button color="primary">Hello</Button>
		</div>
	);
};

export default Home;
